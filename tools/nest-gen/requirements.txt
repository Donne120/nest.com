# nest-gen v2 — Universal Educational Video Engine

# LLM & API
openai>=1.30.0
requests>=2.31.0

# Text-to-Speech (TTS)
edge-tts>=6.1.9

# Image Generation & Processing
Pillow>=10.0.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0

# Video & Audio (optional, for production pipeline)
# Requires ffmpeg to be installed separately: brew install ffmpeg (macOS) or apt install ffmpeg (Linux)

# For Remotion integration (optional)
# nodejs and npm required separately
